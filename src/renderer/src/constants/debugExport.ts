/** Dev-only session trace: sample interval (aligned with gaze metrics tick). */
export const DEBUG_EXPORT_SAMPLE_MS = 1000

/** Dev-only session trace: keep at most the last 10 minutes of samples. */
export const DEBUG_EXPORT_WINDOW_MS = 600_000
